# ==============================================
# TERRA INDUSTRIES BACKEND - ENVIRONMENT VARIABLES
# ==============================================
# Railway Production Configuration
# ==============================================

# ----------------------------------------------
# APPLICATION
# ----------------------------------------------
NODE_ENV=production
PORT=4000

# ----------------------------------------------
# DATABASE (Railway PostgreSQL Plugin)
# ----------------------------------------------
# Railway auto-provides this as ${{Postgres.DATABASE_URL}}
DATABASE_URL=postgresql://postgres:password@host:5432/railway

# ----------------------------------------------
# CACHE (Railway Redis Plugin)
# ----------------------------------------------
# Railway auto-provides this as ${{Redis.REDIS_URL}}
REDIS_URL=redis://:password@host:6379

# ----------------------------------------------
# AUTHENTICATION
# ----------------------------------------------
# Generate secure secret: openssl rand -base64 32
JWT_SECRET=change_this_to_a_secure_random_string_in_production
JWT_EXPIRES_IN=7d

# ----------------------------------------------
# SECURITY & CORS
# ----------------------------------------------
# Update with your frontend URL (Vercel, custom domain, etc.)
CORS_ORIGIN=https://terra-industries-drab.vercel.app
THROTTLE_TTL=60
THROTTLE_LIMIT=10

# ----------------------------------------------
# FILE STORAGE (Cloudflare R2) - Optional
# ----------------------------------------------
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=terra-media
R2_PUBLIC_URL=

# ----------------------------------------------
# EMAIL SERVICE (Resend) - Optional
# ----------------------------------------------
RESEND_API_KEY=
EMAIL_FROM=noreply@terraindustries.com
ADMIN_EMAIL=admin@terraindustries.com

# ----------------------------------------------
# ERROR TRACKING (Sentry) - Optional
# ----------------------------------------------
SENTRY_DSN=

# ----------------------------------------------
# LOGGING
# ----------------------------------------------
LOG_LEVEL=info

# ==============================================
# NOTES FOR RAILWAY DEPLOYMENT
# ==============================================
#
# 1. DATABASE_URL & REDIS_URL are auto-provided by Railway plugins:
#    - Use: ${{Postgres.DATABASE_URL}}
#    - Use: ${{Redis.REDIS_URL}}
#
# 2. Generate JWT_SECRET securely:
#    openssl rand -base64 32
#
# 3. Update CORS_ORIGIN with your frontend URL
#
# 4. Optional services (R2, Resend, Sentry) can be added later
#
# ==============================================

